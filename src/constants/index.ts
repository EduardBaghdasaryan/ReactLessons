const API_URL = "http://localhost:8080";

const ROUTES = {
  SIGN_IN: "/signin",
  HOME: "/home",
  MOVIES: "/movies",
  ADMIN: "/admin",
  MOVIES_BY_ID: "/movies/:movieId",
};

export { API_URL, ROUTES };
